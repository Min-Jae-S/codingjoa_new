<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.codingjoa.mapper.CategoryMapper">

	<!--  
	CREATE TABLE category (
        category_code            NUMBER,                      
        category_parent_code     NUMBER          NULL,         
        category_name            VARCHAR2(50)    NOT NULL,
        CONSTRAINT pk_category PRIMARY KEY(category_code)
        FOREIGN KEY(cate_parent_code) REFERENCES cate_tbl(cate_code)
	);
	-->
	
	<select id="getCategoryList" resultType="list">
		SELECT * FROM category
	</select>
	
</mapper>