<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.codingjoa.mapper.UploadMapper">
<!--  
	board_image_idx         NUMBER,
	board_idx               NUMBER              NULL,
	board_image_name        VARCHAR2(200)       NOT NULL,
    board_image_path        VARCHAR2(200)       NOT NULL,
	regdate                 DATE                NOT NULL,
	
	profile_image_idx         NUMBER,
	member_idx                NUMBER              NULL,
	profile_image_name        VARCHAR2(200)       NOT NULL,
    profile_image_path        VARCHAR2(200)       NOT NULL,
	regdate                   DATE                NOT NULL,
-->

	<insert id="insertBoardImage" parameterType="boardImage">
		<selectKey keyProperty="boardImageIdx" resultType="int" order="BEFORE">
			SELECT seq_board_image.NEXTVAL FROM DUAL
		</selectKey>
		INSERT INTO board_image (
			board_image_idx,
			board_idx,
			board_image_name,
			board_image_path,
			regdate
		) VALUES (
			#{boardImageIdx},
			#{boardIdx},
			#{boardImageName},
			#{boardImagePath},
			SYSDATE
		)
	</insert>
	
	<select id="isBoardImageUploaded" parameterType="int" resultType="boolean">
		SELECT
			COUNT(*)
		FROM
			board_image
		WHERE
			board_image_idx = #{boardImageIdx}
	</select>
	
	<update id="activateBoardImage" parameterType="map">
		UPDATE 
			board_image
		SET
			board_idx = #{boardIdx}
		WHERE
			board_image_idx IN 
			<foreach collection="boardImages" item="boardImageIdx" separator="," open="(" close=")">	
				#{boardImageIdx}
			</foreach>
	</update>
	
	<update id="deactivateBoardImage" parameterType="int">
		UPDATE 
			board_image
		SET
			board_idx = NULL
		WHERE
			board_idx = #{boardIdx} 
	</update>
	
	<select id="findBoardImagesByBoardIdx" parameterType="int" resultType="boardImage">
		SELECT
			*
		FROM
			board_image
		WHERE
			board_idx = #{boardIdx}
	</select>
	
	<update id="deactivateProfileImage" parameterType="int">
		UPDATE 
			profile_image
		SET
			member_idx = NULL
		WHERE
			member_idx = #{memberIdx} 
	</update>
	
	<insert id="insertProfileImage" parameterType="profileImage">
		<selectKey keyProperty="profileImageIdx" resultType="int" order="BEFORE">
			SELECT seq_profile_image.NEXTVAL FROM DUAL
		</selectKey>
		INSERT INTO profile_image (
			profile_image_idx,
			member_idx,
			profile_image_name,
			profile_image_path,
			regdate
		) VALUES (
			#{profileImageIdx},
			#{memberIdx},
			#{profileImageName},
			#{profileImagePath},
			SYSDATE
		)
	</insert>
	
	<select id="findBoardImageByIdx" resultType="boardImage" parameterType="int">
		SELECT 
			* 
		FROM 
			board_image 
		WHERE 
			board_image_idx = #{boardImageIdx}
	</select>
	
</mapper>